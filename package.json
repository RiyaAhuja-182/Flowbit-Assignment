{
  "name": "flowbit-assignment",
  "version": "1.0.0",
  "description": "Multi-service monorepo with React micro frontends and Node.js backend",
  "workspaces": [
    "api",
    "client", 
    "support-tickets-app"
  ],
  "scripts": {
    "dev": "docker-compose up --build",
    "seed": "cd api && npm run seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
